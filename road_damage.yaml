path: C:/Users/ACER/sih2026/dataset/datasetpolehole/train
train: images
val: images # Since we don't have a separate val folder, we'll use train for both for now

nc: 4
names: ['D00', 'D10', 'D20', 'D40']
